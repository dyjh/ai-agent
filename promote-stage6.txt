收到了。你手动上传后的版本已经对上了。

我重新看了你上传的 `README(1).md` 和 `task(1).json`，现在能确认两件事：

1. `README` 已经包含你要求的 **“Eino Workflow / HITL 编排”** 小节，并且明确写了 `internal/einoapp.AgentWorkflow` 的 `Start(ctx, input)` 和 `Resume(ctx, run_id, approval_id, approved)` 语义。
2. `task.json` 也已经切到 **`phase-11-hardening-and-integration`**，当前进度是 **completed 10 / pending 1 / total 11**，说明你前面的 Phase 7～10 已经收口到了文档里。

所以现在下一步就很明确了：**只做 Phase 11**，并把你新增的两个要求压进去：

* 生成 **Swagger/OpenAPI 文档**
* 支持并真正使用这两个 env：

  * `USE_KONWAGE_BASE`
  * `KONWAGE_BASE_PROVIDER=qdrant`

下面是一版可以直接给 Codex 的 **Phase 11 最终详细提示词**。

---

````text
你是一个资深 Go 后端工程师、CloudWeGo Eino Agent 架构师、系统安全工程师、API 文档工程师。请在当前仓库 dyjh/ai-agent 中继续开发，不要推倒重写，只在现有代码基础上完成下一阶段：

Phase 11 - Hardening, Integration, Swagger, Config Finalization

开始前必须先做：

1. 阅读 README.md
2. 阅读 task.json
3. 阅读 AGENTS.md
4. 实际检查代码，而不是只信 README/task.json，重点检查：
   - internal/app/bootstrap.go
   - internal/config/
   - internal/api/router.go
   - internal/api/handlers/
   - internal/api/ws/
   - internal/agent/
   - internal/einoapp/
   - internal/tools/
   - internal/db/
   - cmd/agent-server/main.go
   - cmd/agent/main.go
   - Makefile
   - .env.example
   - config/agent.yaml
   - config/policy.yaml
   - config/mcp.servers.yaml
   - config/mcp.tool-policies.yaml
   - knowledge/
   - tests/
5. 全局搜索：
   - NotImplementedExecutor
   - placeholder
   - TODO
   - registered_only
   - experimental
   - swagger
   - openapi
   - USE_KONWAGE_BASE
   - KONWAGE_BASE_PROVIDER
6. 先输出：
   - 当前 Phase 11 相关问题列表
   - 你准备修改的文件列表
   - 你的实现顺序
7. 然后开始逐步修改代码

重要原则：

- 如果 README/task.json 与代码状态冲突，以代码实现为准，并同步修正文档。
- 不要回退已完成的 Phase 7/8/9/10。
- 不要引入租户、多用户、RBAC、云端 SaaS 逻辑。
- 不要绕过 ToolRouter / EffectInference / PolicyEngine / ApprovalCenter。
- 不要让 Eino Tool 直接操作外部世界。
- 不要泄露 secret、token、cookie、私钥、.env 正文。
- 当前 README 已经包含 “Eino Workflow / HITL 编排” 小节，请保留并在必要时扩充，但不要回退成“未完成”。

==================================================
本阶段总目标
==================================================

完成以下 6 件事：

1. 收口运行时硬化和配置检查
2. 支持新的知识库 env 配置：
   - USE_KONWAGE_BASE
   - KONWAGE_BASE_PROVIDER
3. 完整实现知识库 feature gate
4. 生成 Swagger / OpenAPI 文档并提供 UI
5. 补齐集成测试和运行手册
6. 更新 README.md、.env.example、task.json，并将项目状态收口

==================================================
任务 1：清理 NotImplemented / Placeholder / Experimental 能力暴露
==================================================

请全仓库检查：

- NotImplementedExecutor
- placeholder
- TODO
- registered_only
- 仍会被自动选中的但没有真实执行能力的工具
- README 声称已完成但代码仍未完成的能力

要求：

1. 对已完成的功能：
   - 删除占位实现
   - 删除过期 TODO
   - 确保 README / task.json 同步

2. 对未完成但保留的实验性能力：
   - 必须明确标记 experimental
   - 不允许 Planner 自动选择
   - 不允许 README 或 Swagger 写成“已完成”

3. 对外可见的 ToolSpec：
   - 不能挂一个实际上不执行的 executor
   - 如果暂不可用，要么不注册，要么返回明确 feature_disabled 且不会被自动规划选中

==================================================
任务 2：补齐配置校验与启动检查
==================================================

完善 config loader、bootstrap 和 CLI/server 启动前检查。

至少校验：

- database.url
- qdrant.url（在需要 qdrant provider 时）
- memory.root_dir
- events.jsonl_root
- policy.yaml
- skills.registry.yaml
- mcp.servers.yaml
- mcp.tool-policies.yaml
- knowledge 相关路径
- Swagger 文档相关配置（如适用）

新增配置检查入口，至少实现一个：

- `agent-server --check-config`
- 或 `agent check-config`

要求：

- 缺少必要配置时返回明确错误
- 可自动创建的目录自动创建
- secret 展开失败时不要打印 secret 值
- 启动日志说明：
  - knowledge base enabled/disabled
  - provider
  - docs route
  - config check result
- 日志必须脱敏

==================================================
任务 3：支持新的知识库 env 配置
==================================================

必须按下面两个精确 env 名称支持：

- USE_KONWAGE_BASE
- KONWAGE_BASE_PROVIDER

注意：
- 不要擅自改拼写
- 不要改成你认为更合理的 KNOWLEDGE_BASE 名称后只兼容你自己的新名字
- 这两个原样名称必须生效

行为要求：

1. `USE_KONWAGE_BASE=false`
   - 不初始化 KB runtime 检索链路
   - ContextBuilder 不应使用 KB 检索
   - Planner/Runtime 不应自动选择 kb.search
   - KB API 返回一致的 feature_disabled 错误（推荐 503）
   - `/v1/health` 显示 knowledge_base.enabled=false

2. `USE_KONWAGE_BASE=true`
   - 必须要求 `KONWAGE_BASE_PROVIDER` 有值

3. 当前 provider 只支持：
   - qdrant

4. `USE_KONWAGE_BASE=true` 且 `KONWAGE_BASE_PROVIDER=qdrant`
   - 启用 KB runtime
   - 初始化 Qdrant provider
   - `/v1/health` 显示 enabled=true, provider=qdrant

5. `USE_KONWAGE_BASE=true` 但 provider 缺失或不是 qdrant
   - 启动失败
   - 错误信息清晰
   - 不要静默 fallback 到未知 provider

6. 如果项目内部仍保留 InMemoryVectorIndex 用于测试：
   - 可以继续保留测试路径
   - 但 runtime 的知识库 provider feature gate 必须以这两个 env 为准
   - README 要说明：
     - 当前 runtime provider 只支持 qdrant
     - 内存向量索引用于测试/开发辅助

建议归一化到配置结构，例如：

```go
type KnowledgeBaseConfig struct {
    Enabled  bool
    Provider string
}
````

但 env 名称必须仍然是：

* USE_KONWAGE_BASE
* KONWAGE_BASE_PROVIDER

==================================================
任务 4：收口知识库 feature gate 行为
==========================

请把知识库启用/禁用行为做完整。

要求：

1. 当 KB disabled 时：

   * Runtime / Planner / ContextBuilder 不使用 KB
   * kb.search 不应被自动规划选中
   * 若 HTTP API 被直接调用，返回：

     * code = feature_disabled
     * message 明确说明 knowledge base is disabled
   * `/v1/health` 明确显示 disabled

2. 当 KB enabled 且 provider=qdrant 且服务正常：

   * KB 检索可用
   * `/v1/health` 显示 status=ok

3. 当 KB enabled 但 qdrant 不可用：

   * 推荐启动失败
   * 如果你选择保留降级逻辑，必须非常明确
   * README 必须写清楚真实行为

建议 health 输出：

```json
{
  "knowledge_base": {
    "enabled": true,
    "provider": "qdrant",
    "status": "ok"
  }
}
```

禁用时：

```json
{
  "knowledge_base": {
    "enabled": false,
    "provider": "",
    "status": "disabled"
  }
}
```

==================================================
任务 5：生成 Swagger / OpenAPI 文档
============================

本阶段必须实现：

1. 为 HTTP API 生成机器可读 OpenAPI 文档
2. 提供 Swagger UI
3. 有生成命令
4. README 中给出访问方式

推荐使用 Go 生态成熟方案，例如：

* swaggo/swag + http-swagger
  或等价 OpenAPI 方案

覆盖的接口至少包括：

* GET /v1/health
* POST /v1/conversations
* GET /v1/conversations
* GET /v1/conversations/{conversation_id}
* GET /v1/conversations/{conversation_id}/messages
* POST /v1/conversations/{conversation_id}/messages
* GET /v1/approvals/pending
* POST /v1/approvals/{approval_id}/approve
* POST /v1/approvals/{approval_id}/reject
* GET /v1/memory/files
* GET /v1/memory/files/{path}
* POST /v1/memory/search
* POST /v1/memory/patches
* POST /v1/memory/reindex
* POST /v1/kbs
* GET /v1/kbs
* POST /v1/kbs/{kb_id}/documents/upload
* POST /v1/kbs/{kb_id}/search
* POST /v1/skills/upload
* GET /v1/skills
* GET /v1/skills/{id}
* POST /v1/skills/{id}/enable
* POST /v1/skills/{id}/disable
* POST /v1/skills/{id}/test
* POST /v1/skills/{id}/run
* GET /v1/mcp/servers
* POST /v1/mcp/servers
* GET /v1/mcp/servers/{id}
* PATCH /v1/mcp/servers/{id}
* POST /v1/mcp/servers/{id}/refresh
* POST /v1/mcp/servers/{id}/test
* POST /v1/mcp/servers/{id}/tools/{tool_name}/call
* GET /v1/mcp/tools
* PATCH /v1/mcp/tools/{id}/policy

要求：

* 提供 docs route，例如：

  * `/swagger/index.html`
  * `/swagger/doc.json`
* WebSocket 不强制进 OpenAPI，但 README 必须继续保留 WebSocket 协议说明
* 增加 Makefile 命令，例如：

  * `make swagger`
  * `make docs`
* Swagger 示例值不能包含真实 secret
* 对 feature_disabled 场景要有错误响应说明
* 不要只做静态空壳页面，必须能生成真实文档

==================================================
任务 6：整理 API DTO / 错误响应
======================

为 Swagger 可读性，请补齐或清理 API request/response DTO。

要求：

* 优先使用明确 struct，不要滥用 map[string]any 作为主要对外响应
* JSON tag 完整
* 对动态字段用注释说明
* 统一错误响应格式，例如：

```go
type ErrorResponse struct {
    Code    string `json:"code"`
    Message string `json:"message"`
    Details any    `json:"details,omitempty"`
}
```

知识库未启用示例：

```json
{
  "code": "feature_disabled",
  "message": "knowledge base is disabled",
  "details": {
    "enabled": false,
    "provider": ""
  }
}
```

==================================================
任务 7：补集成测试与硬化测试
===============

至少新增或补齐：

1. config_validation_test.go

   * 必要配置缺失报错
   * env expansion
   * USE_KONWAGE_BASE=false
   * USE_KONWAGE_BASE=true + provider=qdrant
   * invalid provider 报错

2. kb_feature_gate_test.go

   * KB disabled 时 runtime 不使用 KB
   * kb.search API 返回 feature_disabled
   * health 显示 disabled
   * KB enabled + qdrant 可初始化

3. swagger_docs_test.go

   * Swagger route 可访问
   * 文档生成产物存在或生成命令可运行
   * 核心 routes 出现在 doc 中

4. workflow integration tests

   * 只读自动执行
   * 审批暂停 + resume
   * reject 路径
   * 事件顺序

5. postgres/qdrant 集成测试可保留为 env-gated：

   * RUN_INTEGRATION=1
   * RUN_QDRANT_INTEGRATION=1
   * RUN_POSTGRES_INTEGRATION=1

要求：

* 默认 `go test ./...` 不依赖外部服务
* 不要删除现有安全测试

==================================================
任务 8：运行时硬化
==========

补齐以下硬化项：

1. health 输出更完整：

   * database
   * qdrant
   * knowledge_base
   * workflow/runtime
   * version（如方便）

2. approval snapshot integrity

   * 如果还没做 hash，建议加 hash 校验
   * 执行前确认 snapshot 未被篡改

3. 再检查以下输出脱敏：

   * JSONL
   * agent_events
   * structured payload
   * API error
   * Swagger 示例

4. feature_disabled / unsupported_provider / invalid_config / dependency_unavailable 等错误码要明确

==================================================
任务 9：更新 README.md / .env.example / task.json
============================================

README 必须更新并与代码一致，至少包括：

1. 当前完整能力列表
2. Eino Workflow / HITL 编排（必须保留这一节）
3. Swagger / OpenAPI 访问路径
4. Swagger 生成命令
5. 新 env 配置说明：

   * USE_KONWAGE_BASE
   * KONWAGE_BASE_PROVIDER
6. KB 启用/禁用行为
7. 当前 provider 只支持 qdrant
8. WebSocket 说明继续保留
9. 集成测试运行方式
10. 剩余限制（如果还有）

README 中应继续保留并校验这一节：

## Eino Workflow / HITL 编排

当前已提供 `internal/einoapp.AgentWorkflow` facade：

* `Start(ctx, input)`：处理新用户消息，构建上下文、规划、提出工具调用、走 effect/policy/approval/executor 链路
* `Resume(ctx, run_id, approval_id, approved)`：处理审批恢复，不重新规划，不替换 tool input

`.env.example` 必须新增：

```env
USE_KONWAGE_BASE=true
KONWAGE_BASE_PROVIDER=qdrant
QDRANT_URL=http://localhost:6333
```

task.json 必须更新：

* phase-11 status -> completed
* overall_status -> production_ready_mvp 或更准确状态
* completed_items 添加：

  * Swagger/OpenAPI 文档
  * 配置校验收口
  * KB provider feature gate
  * integration / hardening
* change_log 增加本次记录
* unfinished_overview 只保留真正未完成事项
* current_task_id 置为 done、post-mvp-backlog 或等价一致状态

==================================================
建议实现顺序
======

请严格按这个顺序推进：

1. 检查代码与 README/task.json 是否不一致
2. 完善 config loader 和 config check
3. 接入 USE_KONWAGE_BASE / KONWAGE_BASE_PROVIDER
4. 收口 KB feature gate
5. 接入 Swagger/OpenAPI 生成和路由
6. 清理 API DTO / 错误响应
7. 补集成测试与硬化测试
8. gofmt
9. go test ./...
10. 更新 README.md
11. 更新 .env.example
12. 更新 task.json

==================================================
验收标准
====

完成后必须满足：

1. README 保留 Eino Workflow / HITL 编排这一节，并且与代码一致
2. USE_KONWAGE_BASE 和 KONWAGE_BASE_PROVIDER 生效
3. provider 当前仅支持 qdrant，非法 provider 启动时报错
4. KB disabled 时 runtime 不使用 KB，KB API 返回 feature_disabled
5. `/v1/health` 体现 knowledge_base 状态
6. Swagger/OpenAPI 可访问
7. 有生成命令，有文档产物或可重复生成流程
8. `.env.example` 已更新
9. `go test ./...` 通过
10. task.json 收口到 Phase 11 完成
11. 没有新增绕过 ToolRouter / Policy / Approval 的路径

==================================================
特别注意
====

* 必须按这两个精确 env 名称支持：

  * USE_KONWAGE_BASE
  * KONWAGE_BASE_PROVIDER
* 不要擅自改名
* 不要让 KB disabled 只是“看起来关闭”，但 runtime 仍偷偷检索
* 不要只加 Swagger route，不生成真实文档
* 不要让 README 或 Swagger 领先于实现
* 不要引入租户、多用户、RBAC

现在开始执行。

先输出：

1. 当前 Phase 11 问题列表
2. 计划修改的文件列表
3. 实现顺序

然后再开始逐步修改，最后给出：

* 测试结果
* Swagger 生成与访问摘要
* README 更新摘要
* .env.example 更新摘要
* task.json 更新摘要

```

现在这版是基于你**手动上传后的 README 和 task.json** 写的，不再假设 Workflow 还没完成，而是直接进入 **Phase 11 收口**。README 里你要求的那段 `AgentWorkflow` 说明也已经在上传版本中存在了。
```
